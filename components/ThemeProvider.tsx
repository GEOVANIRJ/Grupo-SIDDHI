"use client";

// Deprecated: Theme initialization is handled by `context/DarkModeContext.tsx`.
// This component is kept as a no-op to avoid breaking imports during hot-reloads.
export default function ThemeProvider() {
  return null;
}
